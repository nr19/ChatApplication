# A simple chatApplication between a client and a server.

To run the application, create 2 tabs separately one for client and server. Then run `dotnet run` for each. This will start the server. 

Now, type something in the client terminal. You can see that the same message can be seen in the server terminal. 
